#!/usr/bin/env python3
"""Slot OAuth init mode for codex-shell pods (WOVED-126 + WOVED-128).

One-shot init pod that drives the Claude Code device-code OAuth flow
under operator supervision via the woveD Manager. This is the agent-pod
half of the dance described in Confluence page 65961985 and implemented
on the Manager side in `slot_auth.py` + the `/slots/<id>/auth-init/*`
callback endpoints.

Lifecycle:

    1. Spawn `claude` in a pseudo-terminal so it behaves interactively
       the way it does at a real shell.
    2. Watch its stdout for the OAuth device-code URL pattern.
       Anthropic's CLI prints something like:
           To continue, please visit: https://...
           and enter the code: ABCD-1234
    3. POST URL + user_code to Manager at
       /slots/<SLOT_ID>/auth-init/url with X-Slot-Init-Token header.
    4. Poll /slots/<SLOT_ID>/auth-init/code (also with token) until
       the operator submits the code via the dashboard. Backoff: 2s
       between polls; cap at AUTH_INIT_TIMEOUT_S total.
    5. Type the code into the PTY so `claude` can complete login.
    6. Wait for the agent process to exit. Verify ~/.claude/ has auth
       state. Exit 0 on success.

WOVED-128: every callback request includes the per-slot bearer token
in the X-Slot-Init-Token header. Without it the Manager returns 401.
The token is generated by the Manager when the init pod is spawned
and injected as WOVED_SLOT_INIT_TOKEN env. A sibling pod that can
reach the Manager service can NOT poll another slot's URL or consume
its code without the matching token.

Stdlib only — no extra deps to vendor into the image. Uses `pty` +
`os.read`/`os.write` because that's what works portably; `pexpect`
would be slightly nicer but adds a dep that's not worth pinning into
the image just for one script.

Operator failure modes:
    - Timeout — operator never pastes the code. Init pod exits
      non-zero; Manager reconciler marks the slot ERROR.
    - Bad code — claude rejects, prints the URL again. The script
      treats this as a fresh URL surface and re-posts it (idempotent).
    - Agent crashes — stdout closes; script exits with the agent's
      return code.

This script is the FIRST DRAFT — `claude /login`'s exact CLI shape +
prompt patterns may need adjustment after a real-pod test pass. The
TODO markers below call out the parts most likely to need iteration.
"""

from __future__ import annotations

import json
import os
import pty
import re
import select
import sys
import time
import urllib.error
import urllib.parse
import urllib.request
from datetime import datetime, timezone

# Env contract — set by the chart's slot-init Pod manifest at spawn time.
SLOT_ID = os.environ.get("WOVED_SLOT_ID", "")
TASK_AGENT = os.environ.get("WOVED_TASK_AGENT", os.environ.get("AGENT", ""))
CALLBACK_URL = os.environ.get("WOVED_MANAGER_CALLBACK_URL", "").rstrip("/")
SLOT_INIT_TOKEN = os.environ.get("WOVED_SLOT_INIT_TOKEN", "")
AUTH_INIT_TIMEOUT_S = int(os.environ.get("WOVED_AUTH_INIT_TIMEOUT_S", "1800"))  # 30 min default
POLL_INTERVAL_S = int(os.environ.get("WOVED_AUTH_INIT_POLL_S", "2"))

# OAuth URL extraction. Anthropic's flow prints the device URL as a
# plain https link; we accept any https URL on a line that mentions
# `code` or `visit` or `verify` to stay tolerant of CLI version drift.
# TODO(WOVED-126): tighten this once we've seen real `claude /login`
# output and know the exact format.
URL_PATTERN = re.compile(rb"(https://[^\s\"'<>)]+)", re.IGNORECASE)
USER_CODE_PATTERN = re.compile(rb"\b([A-Z0-9]{4,8}-?[A-Z0-9]{4,8})\b")

# Per-agent invocation. Empty for codex — codex doesn't use OAuth, so
# this script is claude-only. If/when Gemini gets an OAuth path, add it
# here with its CLI shape.
AGENT_INIT_CMDS = {
    # TODO(WOVED-126): confirm this is the right invocation. If
    # `claude /login` is a slash command (REPL only), spawn `claude`
    # bare and type `/login\n` after the REPL prompt appears.
    "claude": ["claude"],
}


def _die(msg: str, code: int = 1) -> None:
    print(f"auth-init: FATAL: {msg}", file=sys.stderr)
    sys.exit(code)


def _validate_env() -> None:
    missing = [
        name
        for name, val in (
            ("WOVED_SLOT_ID", SLOT_ID),
            ("WOVED_TASK_AGENT", TASK_AGENT),
            ("WOVED_MANAGER_CALLBACK_URL", CALLBACK_URL),
            ("WOVED_SLOT_INIT_TOKEN", SLOT_INIT_TOKEN),
        )
        if not val
    ]
    if missing:
        _die(f"missing required env vars: {', '.join(missing)}")
    if TASK_AGENT not in AGENT_INIT_CMDS:
        _die(
            f"agent {TASK_AGENT!r} does not need OAuth init "
            f"(supported: {sorted(AGENT_INIT_CMDS)})"
        )


def _http_post(url: str, body: dict) -> int:  # type: ignore[type-arg]
    """POST with the per-slot bearer token in the X-Slot-Init-Token
    header. Required by Manager-side auth (WOVED-128); without it the
    Manager returns 401."""
    data = json.dumps(body).encode("utf-8")
    req = urllib.request.Request(
        url,
        data=data,
        method="POST",
        headers={
            "Content-Type": "application/json",
            "X-Slot-Init-Token": SLOT_INIT_TOKEN,
        },
    )
    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            resp.read()
            return resp.status
    except urllib.error.HTTPError as exc:
        return exc.code
    except urllib.error.URLError as exc:
        print(f"auth-init: POST {url} → transport error: {exc}", file=sys.stderr)
        return 0


def _http_get(url: str) -> tuple[int, dict | None]:  # type: ignore[type-arg]
    """GET with the per-slot bearer token in the X-Slot-Init-Token
    header. WOVED-128 requirement same as POST."""
    req = urllib.request.Request(
        url,
        headers={"X-Slot-Init-Token": SLOT_INIT_TOKEN},
    )
    try:
        with urllib.request.urlopen(req, timeout=10) as resp:
            return resp.status, json.loads(resp.read())
    except urllib.error.HTTPError as exc:
        try:
            return exc.code, json.loads(exc.read())
        except (json.JSONDecodeError, ValueError):
            return exc.code, None
    except urllib.error.URLError as exc:
        print(f"auth-init: GET {url} → transport error: {exc}", file=sys.stderr)
        return 0, None


def _post_url(url: str, user_code: str | None) -> None:
    body: dict = {"url": url}  # type: ignore[type-arg]
    if user_code:
        body["user_code"] = user_code
    target = f"{CALLBACK_URL}/slots/{urllib.parse.quote(SLOT_ID, safe='')}/auth-init/url"
    rc = _http_post(target, body)
    if rc not in (200, 204):
        print(f"auth-init: WARN: POST URL → HTTP {rc}", file=sys.stderr)
    else:
        print(f"auth-init: posted URL to {target}", file=sys.stderr)


def _poll_for_code(deadline: float) -> str | None:
    """Long-poll the Manager for the operator-submitted auth code.

    Returns the code string when received. Returns None on timeout —
    caller should kill the agent process + exit non-zero so the
    Manager reconciler marks the slot ERROR.
    """
    target = f"{CALLBACK_URL}/slots/{urllib.parse.quote(SLOT_ID, safe='')}/auth-init/code"
    while time.time() < deadline:
        status, body = _http_get(target)
        if status == 200 and body and "code" in body:
            print("auth-init: received code from Manager", file=sys.stderr)
            return str(body["code"])
        if status == 401:
            # Token rejected — likely env wiring bug. Logging it loudly
            # so an operator sees this in the Job log and knows to look
            # at the Pod manifest's WOVED_SLOT_INIT_TOKEN env.
            print(
                "auth-init: WARN: Manager returned 401 — check "
                "WOVED_SLOT_INIT_TOKEN env vs Manager's registered token",
                file=sys.stderr,
            )
            time.sleep(POLL_INTERVAL_S)
            continue
        if status == 404:
            time.sleep(POLL_INTERVAL_S)
            continue
        # Any other status: log + back off + retry.
        print(f"auth-init: WARN: GET code → HTTP {status}, retrying", file=sys.stderr)
        time.sleep(POLL_INTERVAL_S)
    print(f"auth-init: timed out waiting for code after {AUTH_INIT_TIMEOUT_S}s", file=sys.stderr)
    return None


def _drive_login() -> int:
    """Spawn `claude` under a PTY, walk the OAuth dance, return its
    exit code. Long function on purpose — the state machine is
    inherently sequential and splitting it makes the flow harder to
    follow than the inline form."""
    cmd = AGENT_INIT_CMDS[TASK_AGENT]
    print(f"auth-init: spawning {cmd}", file=sys.stderr)

    pid, fd = pty.fork()
    if pid == 0:  # child
        # exec the agent CLI; if it fails, the parent reads EOF.
        os.execvp(cmd[0], cmd)
        return 0  # unreachable

    # Parent: drive the dance.
    deadline = time.time() + AUTH_INIT_TIMEOUT_S
    url_posted = False
    code_typed = False
    buffer = b""
    rep_prompt_seen = False

    try:
        while True:
            # Has the child died?
            wpid, status = os.waitpid(pid, os.WNOHANG)
            if wpid != 0:
                rc = os.WEXITSTATUS(status) if os.WIFEXITED(status) else 1
                print(f"auth-init: agent process exited rc={rc}", file=sys.stderr)
                return rc

            # Bound the wait so we can re-check waitpid + deadline.
            ready, _, _ = select.select([fd], [], [], 0.5)
            if fd in ready:
                try:
                    chunk = os.read(fd, 4096)
                except OSError:
                    chunk = b""
                if not chunk:
                    # PTY closed; child exited.
                    continue
                # Tee to our stderr so the Job log captures it.
                sys.stderr.buffer.write(chunk)
                sys.stderr.flush()
                buffer += chunk

                if not url_posted:
                    # Look for the URL.
                    url_match = URL_PATTERN.search(buffer)
                    if url_match:
                        url = url_match.group(1).decode("utf-8", errors="replace")
                        # Look for an adjacent user code (best-effort).
                        code_match = USER_CODE_PATTERN.search(buffer)
                        user_code = (
                            code_match.group(1).decode("utf-8", errors="replace")
                            if code_match
                            else None
                        )
                        _post_url(url, user_code)
                        url_posted = True
                        buffer = b""  # clear so we don't re-match the same URL

                # If claude /login is a REPL slash command, look for the
                # first prompt and inject /login. TODO(WOVED-126):
                # confirm whether this is needed; remove the branch if
                # `claude` auto-prompts OAuth on no-auth-state startup.
                if not rep_prompt_seen and not url_posted and (b">" in buffer or b"$" in buffer):
                    print("auth-init: REPL prompt detected, sending /login", file=sys.stderr)
                    os.write(fd, b"/login\n")
                    rep_prompt_seen = True
                    buffer = b""

            if url_posted and not code_typed:
                code = _poll_for_code(deadline)
                if code is None:
                    # Timed out. Kill the agent and bubble up.
                    try:
                        os.kill(pid, 15)  # SIGTERM
                        os.waitpid(pid, 0)
                    except ProcessLookupError:
                        pass
                    return 124  # conventional timeout exit
                # Type the code into the PTY. Trailing \n delivers it.
                os.write(fd, code.encode("utf-8") + b"\n")
                code_typed = True
                print("auth-init: code piped into agent stdin", file=sys.stderr)

            if time.time() > deadline:
                print("auth-init: deadline exceeded", file=sys.stderr)
                try:
                    os.kill(pid, 15)
                    os.waitpid(pid, 0)
                except ProcessLookupError:
                    pass
                return 124
    finally:
        try:
            os.close(fd)
        except OSError:
            pass


def _snapshot_claude_dir() -> dict[str, tuple[int, float]]:
    """Return a snapshot of ~/.claude/ contents: {relpath: (size, mtime)}.

    WOVED-131: the entrypoint pre-populates ~/.claude/ from
    image-baked defaults + the agent-config CLAUDE.md symlink BEFORE
    AGENT_MODE=auth-init runs. A naive "any file under ~/.claude/
    means success" check would false-positive even when claude exits
    without writing real credentials. The fix is to snapshot
    before + after the login dance and assert the after-set differs
    from the before-set (new files OR existing files with changed
    size/mtime). Symlinks are excluded — CLAUDE.md is a stable
    symlink to agent-config that would otherwise show false
    differences across runs."""
    claude_dir = os.path.expanduser("~/.claude")
    snapshot: dict[str, tuple[int, float]] = {}
    if not os.path.isdir(claude_dir):
        return snapshot
    for root, dirs, files in os.walk(claude_dir, followlinks=False):
        # Skip the dir itself if it's a symlink (defense in depth).
        for name in files:
            full = os.path.join(root, name)
            try:
                st = os.lstat(full)
            except OSError:
                continue
            # Skip symlinks — CLAUDE.md is a symlink to agent-config
            # that mtime-jitters on every boot.
            import stat as _stat
            if _stat.S_ISLNK(st.st_mode):
                continue
            rel = os.path.relpath(full, claude_dir)
            snapshot[rel] = (st.st_size, st.st_mtime)
    return snapshot


def _diff_auth_artifacts(
    before: dict[str, tuple[int, float]],
    after: dict[str, tuple[int, float]],
) -> list[str]:
    """Return the sorted list of new + modified file paths between the
    pre-login and post-login snapshots of ~/.claude/. Empty = no
    evidence of a successful login (the WOVED-131 false-positive
    guard: the entrypoint pre-populates ~/.claude/ from
    image-baked defaults, so the absolute-presence check we used
    before was tripping even when claude exited without writing
    real credentials).

    Caller in main() uses the empty/non-empty status as the success
    gate AND forwards the artifact list to the Manager's
    /slots/<id>/auth-init/complete callback (WOVED-147) so the
    refresh-token monitor + dashboard can show which files landed
    without needing to kubectl exec into the pod."""
    new_files = sorted(set(after) - set(before))
    if new_files:
        print(
            f"auth-init: new file(s) under ~/.claude/: {new_files}",
            file=sys.stderr,
        )
    modified = sorted(
        rel for rel in set(after) & set(before) if after[rel] != before[rel]
    )
    if modified:
        print(
            f"auth-init: modified file(s) under ~/.claude/: {modified}",
            file=sys.stderr,
        )
    return new_files + modified


def _verify_new_auth_artifacts(
    before: dict[str, tuple[int, float]],
    after: dict[str, tuple[int, float]],
) -> bool:
    """True iff `_diff_auth_artifacts` finds any new-or-modified file.
    Thin wrapper kept for backward-compatible callers / tests."""
    return bool(_diff_auth_artifacts(before, after))


def _post_complete(artifacts: list[str]) -> None:
    """POST the auth-completion notification to the Manager (WOVED-147).

    Manager records a `slot_auth_completed` audit event with the
    artifact list + timestamp. The refresh-token monitor reads those
    events later to compute time-to-expiry per slot and fire
    `[for-nate] re-auth slot <ID>` Plane tickets ~7 days before the
    upstream refresh token expires.

    Best-effort: a failed POST is logged but doesn't fail the init.
    The slot is still operational (credentials are on the PVC); only
    the expiry-monitoring loop loses one signal. Cases the caller
    accepts as soft-fail:
      - 404: older Manager that doesn't have the endpoint yet. The
        WOVED-147 PR sequence ships Manager-side first (woved#86)
        and codex-shell second; a pod built from this commit
        against a pre-#86 Manager will see 404 and move on.
      - 5xx / transport: Manager pod restart mid-call, network
        hiccup. Auth state is durable on the PVC; the next slot
        rotation re-fires the event when the slot is re-init'd.

    Send the full artifact list — the Manager-side handler
    (woved#86) does the truncation + records `artifacts_truncated_from`
    on the audit row. Client-side capping would silently lose the
    original count because the Manager only sets the truncation field
    when ITS view of the list exceeds the cap. Codex flagged this in
    cross-review of the first cut of this PR."""
    target = f"{CALLBACK_URL}/slots/{SLOT_ID}/auth-init/complete"
    body: dict[str, object] = {
        "completed_at": datetime.now(timezone.utc).isoformat(),
        "agent": TASK_AGENT,
        "artifacts": artifacts,
    }
    rc = _http_post(target, body)
    if rc == 204:
        print("auth-init: posted /complete to Manager", file=sys.stderr)
        return
    if rc == 404:
        print(
            "auth-init: Manager has no /complete endpoint yet "
            "(WOVED-147 still rolling out) — skipping",
            file=sys.stderr,
        )
        return
    # Anything else is best-effort soft-fail; init pod stays exit 0
    # because the credential file already landed on the PVC.
    print(
        f"auth-init: /complete POST returned {rc} — refresh-token "
        "monitor will be missing one slot_auth_completed event "
        "(slot is still operational)",
        file=sys.stderr,
    )


def main() -> int:
    _validate_env()
    # WOVED-131: snapshot ~/.claude/ before the login dance so the
    # post-login verify can prove that real credential files appeared
    # (vs. just the entrypoint's pre-populated defaults).
    before = _snapshot_claude_dir()
    rc = _drive_login()
    if rc != 0:
        print(f"auth-init: agent exited non-zero ({rc})", file=sys.stderr)
        return rc
    after = _snapshot_claude_dir()
    artifacts = _diff_auth_artifacts(before, after)
    if not artifacts:
        print(
            "auth-init: agent exited 0 but no NEW or MODIFIED files under "
            "~/.claude/ — treating as failure (entrypoint pre-populates "
            "defaults; WOVED-131 requires that the login dance produce "
            "evidence beyond what was already there)",
            file=sys.stderr,
        )
        return 1
    # WOVED-147: notify the Manager so the refresh-token monitor's
    # audit stream records the completion. Best-effort — failure
    # doesn't block the init since credentials already landed on the
    # PVC; the next rotation will refire the event.
    _post_complete(artifacts)
    print("auth-init: success — slot is ready for tasks", file=sys.stderr)
    return 0


if __name__ == "__main__":
    sys.exit(main())
